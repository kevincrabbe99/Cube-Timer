//
//  SolvesGridView.swift
//  Cube Timer
//
//  Created by Kevin Crabbe on 12/25/20.
//

import SwiftUI
/*
struct SolvesGridView: View {
    
    var controller: SolvesGridController
    
    var body: some View {
        LazyVStack {
            /*
            ForEach(controller.organizedSolves) { os in
                os.value.getView()
            } // left off here, using solvesData.getApplicableTimeGroups().sGroup to update this view
            */
/*
            ForEach(controller.timeGroups, id: \.self) { tg in
                this will loop through controller.TimeGroupControllers
            }
 */
            Text("in progress")
 
            
            //have to find out how to loop through a key value pair
        }
    }
}

struct SolvesGridView_Previews: PreviewProvider {
    static var previews: some View {
        SolvesGridView(controller: SolvesGridController())
    }
}
*/
